package main.geeksforgeek.dp.partition;


public class SolveWithMemorization extends PartitionSolver {
    @Override
    protected boolean isPartionable(int[] array, int from, int to) {

        return true;
    }
}
